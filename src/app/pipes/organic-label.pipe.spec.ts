import { OrganicLabelPipe } from './organic-label.pipe';

describe('OrganicLabelPipe', () => {
  it('create an instance', () => {
    const pipe = new OrganicLabelPipe();
    expect(pipe).toBeTruthy();
  });
});
